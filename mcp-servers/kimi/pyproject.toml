[project]
name = "builtpro-kimi-mcp"
version = "0.1.0"
description = "Kimi AI (Moonshot) MCP server for BuilTPro Brain AI"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
